import java.awt.image.BufferedImage;
import javax.imageio.ImageIO;
import java.io.File;

import java.awt.*;

public class GraphicStage extends Frame {
    private String name;

    // public GraphicStage() {

    // setSize(1000, 1000);
    // }

    public GraphicStage(int w, int h) {
        setSize(w, h);
    }

    // public void saveAsPNG() {
    // // BufferedImage bi = new BufferedImage(this.getWidth(), this.getHeight(),
    // // BufferedImage.TYPE_INT_ARGB);
    // Graphics2D g = this.getGraphics();
    // ImageIO.write(g,"PNG",new File("/Ressources/"+this.name".png"))
    // }

}
